import Logo from "../logo";
const CustomHeader = () => {
  return (
    <div
      style={{
        height: "80px",
        width: "100%"
      }}
    >
      <Logo />
    </div>
  );
};

export default CustomHeader;
